// Loaded via <script src> for the same file:// compatibility reason as
// bootstrap-config.js. NEYNAR_API_DOCS is Neynar's public demo key (heavily
// rate-limited) - get your own free-tier key at https://neynar.com and put
// it here for real use. It's exposed to anyone viewing the page source,
// same as the demo key, since this all runs client-side.
window.SOCIAL_IMPORT_CONFIG = {
    neynarApiKey: 'NEYNAR_API_DOCS'
};
